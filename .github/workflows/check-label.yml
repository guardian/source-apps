name: Check PR Labels

on:
  pull_request:
    types: [opened, labeled, unlabeled]

permissions:
  pull-requests: write

jobs:
  check-labels:
    name: Check Required Labels
    uses: guardian/check-labels/.github/workflows/check-labels.yml@ab/create-workflow
    with:
      additional-labels: 'experiment'